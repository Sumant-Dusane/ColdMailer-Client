import { FormSchema } from "@/popup/main_form";

export const getColdMail = async (info: String): Promise<String> => {
    return info;
}


export const generateAiMail = async (formData: FormSchema): Promise<String> => {
    console.log(formData);
    return "";
}
